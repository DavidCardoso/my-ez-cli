#!/bin/bash
set -e

# Source common utilities (works with symlinks)
SCRIPT_DIR="$(cd "$(dirname "$([ -L "$0" ] && readlink "$0" || echo "$0")")" && pwd)"
MEC_BASE_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
. "${MEC_BASE_DIR}/bin/utils/common.sh"

# Check Docker availability
check_docker

IMAGE=${IMAGE:-"ghcr.io/promptfoo/promptfoo:latest"} # pin a specific version if needed
WORKDIR=/app

# Container naming and labeling (promptfoo-server runs persistently, so we use a predictable name)
TIMESTAMP=$(date +%s)
PROMPTFOO_CONTAINER_NAME=mec-promptfoo-server-${PROMPTFOO_CONTAINER_SUFFIX:-"$TIMESTAMP"}
CONTAINER_LABELS=$(get_container_labels "promptfoo-server" "$IMAGE")
PROMPTFOO_CONFIG_DIR=${PROMPTFOO_CONFIG_DIR:-"$PWD/data"} # folder to store promptfoo config and data
PROMPTFOO_API_PORT=${PROMPTFOO_API_PORT:-33333} # default API port

# Model providers API Keys - required only for running the tests directly from the server (e.g., Web UI)
OPENAI_API_KEY=${OPENAI_API_KEY:-""}
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-""}
AZURE_API_KEY=${AZURE_API_KEY:-""}
LITELLM_API_KEY=${LITELLM_API_KEY:-""}
GITHUB_TOKEN=${GITHUB_TOKEN:-""}

# AWS config - for bedrock and bedrock-agent providers
AWS_REGION=${AWS_REGION:-""}
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-""}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-""}
AWS_SESSION_TOKEN=${AWS_SESSION_TOKEN:-""} # when using temporary credentials
LANG=${LANG:-"en_US.UTF-8"}

if [ ! -z "$PROMPTFOO_API_PORT" ]; then
    # Default port binding for promptfoo API and UI
    PORTS="-p $PROMPTFOO_API_PORT:$PROMPTFOO_API_PORT"
fi

# Setup logging (if enabled)
setup_logging "promptfoo-server" "$IMAGE" "promptfoo-server ${*}"

# should be executed from your working directory
docker run -d \
    --name $PROMPTFOO_CONTAINER_NAME \
    $CONTAINER_LABELS \
    --volume $PROMPTFOO_CONFIG_DIR:$WORKDIR/data \
    --env PROMPTFOO_CONFIG_DIR=$WORKDIR/data \
    --env API_PORT=$PROMPTFOO_API_PORT \
    --env AWS_REGION=$AWS_REGION \
    --env AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID \
    --env AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY \
    --env AWS_SESSION_TOKEN=$AWS_SESSION_TOKEN \
    --env LANG=$LANG \
    --env OPENAI_API_KEY=$OPENAI_API_KEY \
    --env ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY \
    --env AZURE_API_KEY=$AZURE_API_KEY \
    --env LITELLM_API_KEY=$LITELLM_API_KEY \
    --env GITHUB_TOKEN=$GITHUB_TOKEN \
    $PORTS \
    $IMAGE "${@}"

echo ">>> Promptfoo server is running: run 'docker ps |grep $PROMPTFOO_CONTAINER_NAME' for more info <<<"
