AWS SSO Credentials Getter via Docker
--

[See more](https://github.com/PredictMobile/aws-sso-credentials-getter/).

# Building the image

1. Change the `NODE_VERSION` env var in the `Dockerfile` file if needed
2. Run the `./build` script
